---
title: "<center>Лабораторна робота № 3</center>"
output:
  html_document:
    df_print: paged
fontsize: 14pt
editor_options:
  chunk_output_type: inline
---


**Тема.** Основи вибіркового методу

**Мета:** засвоїти основи статистичного оцінювання характеристик випадкової величини на основі вибіркового підходу засобами мови програмування R; набути навичок роботи у середовищі RStudio із застосуванням концепції «грамотного програмування» із застосуванням пакету R Markdown.

<center>**Хід Роботи**</center>

Генеруємо вибірку з нормального розподілу з параметрами a = 0, σ = 2 об’єму n = 200:

```{r}
a <- 0
s <- 2
n <- 200
n3 <- 1000 
```

Генеруємо вибірку нормального розподілу

```{r, echo=TRUE, message=FALSE, warning=FALSE}
set.seed(0) 
X <- rnorm(n, a, s)
X3 <- rexp(n3, rate = 3)
cat("\n", "Параметри вибірки X:", "\n", "a = ", a, "\n" , "s = ", s, "\n" , "n = ", n, "\n")
cat("Перші елементи вибірки X:", head(X))
cat("Останні елементи вибірки X:", tail(X))

cat("\n", "Параметри вибірки X3:", "\n", "a = ", a, "\n" , "s = ", s, "\n" , "n =  ", n3, "\n")
cat("Перші елементи вибірки X3:", head(X3))
cat("Останні елементи вибірки X3:", tail(X3))
```

Вивід сумарних статистик

```{r}
summary(X)
```

Скомпонуємо результати у компактний варіант виводу основних графіків вибіркових характеристик

```{r}
op <- par(mfrow = c(2,2))
Fn <- ecdf(table(cut(X, nclass.Sturges(X))))
Fn <- ecdf(X)
  
hist(X,
freq = FALSE,
col = "Lightgray",
main="Histogram",
border=4)
curve(dnorm(x, a, s),
col = 2,
lty = 2,
lwd = 2,
add = TRUE)

plot(Fn,
main = "Quantile Plot",
verticals = TRUE,
col.points = "blue",
col.hor = "red",
col.vert = "bisque",
xlab = "X",
ylab = "Fn(x)")

boxplot(X,
main = "Box-and-Wisker Plot",
col = "Lightgray",
border = 4,
xlab = "X",
ylab = "",
horizontal = TRUE) 

stripchart(X, method = "jitter", col = 2, add = TRUE) 

plot(density(X, adjust=2),
main = "density trace",
xlab = "X",
ylab = "Dencity",
col="blue")
rug(X, ticksize = 0.01, col="blue")
```

```{r echo=FALSE}
par(op)

# Центральні моменти
mu_3 <- mean((X - mean(X))^3) 
mu_4 <- mean((X - mean(X))^4)  

# Асиметрія та ексцес
A_s <- mu_3 / (sd(X)^3) 
E_k <- mu_4 / (sd(X)^4) - 3  

# Центральні моменти вибірки X3
mu_3_X3 <- mean((X3 - mean(X3))^3)
mu_4_X3 <- mean((X3 - mean(X3))^4)
A_s_X3 <- mu_3_X3 / (sd(X3)^3)
E_k_X3 <- mu_4_X3 / (sd(X3)^4) - 3
```

Результати моделювання зводимо у таблицю

```{r}
library(knitr)
results <- data.frame(
  Характеристика = c("Математичне сподівання", "Дисперсія", "Виправлена дисперсія", 
                     "СКВ", "Виправлене СКВ", "Центральний момент 3-го порядку",
                     "Центральний момент 4-го порядку", "Асимметрія", "Ексцес"),
  Теоретичне_значення = c(a, s^2, NA, s, NA, NA, NA, NA, NA),
  Вибірка_X = c(mean(X), var(X), var(X) * n / (n - 1), sd(X), sqrt(var(X) * n / (n - 1)),
                mu_3, mu_4, A_s, E_k),
  Вибірка_X3 = c(mean(X3), var(X3), var(X3) * n3 / (n3 - 1), sd(X3), 
                 sqrt(var(X3) * n3 / (n3 - 1)), mu_3_X3, mu_4_X3, A_s_X3, E_k_X3)
)

kable(results, caption = "Результати обчислень")
```
<center>**Відповіді на контрольні питання**</center>
1. **Що таке вибірка?**

Вибірка — це підмножина елементів з усієї популяції, яку обирають для проведення статистичних аналізів. Вибірка дозволяє зробити висновки щодо характеристик популяції, не потребуючи дослідження кожного її елемента. Вибірки можуть бути:

- **Справедливими (random)** — коли кожен елемент популяції має рівну ймовірність потрапити у вибірку.
- **Стратегіями (stratified)** — коли популяція ділиться на підгрупи (страти), і вибірка формується з кожної підгрупи окремо.

2. **Як можна оцінити вибіркове математичне сподівання?**

Вибіркове математичне сподівання або середнє значення вибірки обчислюється як середнє арифметичне всіх елементів вибірки. Для цього всі значення вибірки сумуються, а потім поділяються на кількість елементів у вибірці.

3. **Які існують міри розсіювання?**

Міри розсіювання дають змогу оцінити варіативність або відхилення даних від їхнього середнього значення. Основні міри:

- **Дисперсія ($𝜎^2$)**: вимірює середньоквадратичне відхилення кожного елементу вибірки від середнього значення.
- **Середнє квадратичне відхилення (СКВ, σ)**: це квадратний корінь з дисперсії, що вказує на середнє відхилення значень від середнього.
- **Розмах**: це різниця між найбільшим і найменшим значенням у вибірці.

4. **Які міри форми розподілу вам відомі? Що вони характеризують? Як їх оцінити на практиці?**

Міри форми розподілу дають уявлення про те, як розподілені дані в межах вибірки:

- **Асиметрія (Skewness)**: вказує на напрямок асиметрії розподілу. Якщо асиметрія більша за нуль, то розподіл має правий хвіст, якщо менша за нуль — лівий хвіст.
- **Ексцес (Kurtosis)**: визначає, наскільки розподіл є піковим або плоским порівняно з нормальним розподілом. Ексцес більше 3 вказує на пік, менше — на плаский розподіл.

Для обчислення асиметрії та ексцесу на практиці часто використовуються статистичні програми, як-от R або Python (з бібліотекою `scipy.stats` для Python, функціями `skew()` та `kurtosis()` в R).




